# KEY-364 확정 시연 흐름 전수 점검

점검일: 2026-09-17  
최초 기준 소스: `develop` `ad1d2af`  
수정 기준 소스: `develop` `726f795`  
재개 시 `git fetch origin develop`으로 동일 HEAD 확인. 배포 전체 SHA는 미확인.  
배포 주소: `https://care-on.site/`  
기존 로컬 화면: `http://192.168.219.103:8080/`  
수정본 분리 미리보기: `http://192.168.219.103:8081/`

## 확정 범위

병원 로그인 → 환자 등록 → 진료기록·의료문서 업로드 → OCR 확인·수정·확정 →
RAG 기반 안내문 생성 → 의사 검토·승인 → 환자 문자 발송 → 환자 OTP 로그인 →
안내 내용·챗봇 확인 → 만족도·오류 피드백 → 관리 화면의 피드백 확인.

D+7 복약·통증 확인과 이후 확인 문자 회차는 이번 시연에서 제외한다. 기존 코드는
유지하되 시연 동선에서 약속하거나 진입을 유도하면 안 된다.

## 발견 항목

### KEY-364-F01 — 최종 확인에서 제외 범위인 확인 문자 설정이 노출됨

- 심각도: P0 — 확정 시연 범위와 직접 충돌
- 역할: 스탭·의사
- 경로: 환자 목록 → 환자 선택 → 최종 확인 → 문자 설정
- 재현:
  1. 승인 대기 진료를 연다.
  2. `최종 확인`을 누른다.
  3. `문자 설정`을 누른다.
- 기대: 진료 당일 안내문 승인·발송 정보만 보이고 D+7 이후 확인 문자 진입점은 없다.
- 실제: `일주일 뒤`, `보름 뒤`, `한 달 뒤`, `복약 7일째 확인`, 확인 문자 시각과
  템플릿 편집 UI가 노출된다.
- 근거 소스: `frontend/js/guide-view.js`, `frontend/js/sms-plan.js`
- 상태: 이전 배포 스탭 화면에서 관찰. 로컬 동일 재현은 미검증. `KEY-366`으로 분리

### KEY-364-F02 — 현황 화면이 제외 범위 회차와 응답을 약속함

- 심각도: P0 — 시연자가 후속 문자가 실제 시연 범위라고 오해함
- 역할: 스탭·의사
- 경로: 환자 목록 → 환자 선택 → 현황
- 재현:
  1. 승인된 진료를 연다.
  2. `현황`을 누른다.
- 기대: 진료 안내문 발송·열람·환자 링크 상태만 표시한다.
- 실제: `일주일 뒤 확인`, `보름 뒤` 예약과 `확인 문자 응답` 영역이 표시된다.
- 근거 소스: `frontend/js/status-view.js`, `frontend/js/message-words.js`
- 상태: 이전 로컬 의사 화면에서 관찰. 실행 버전은 확인하지 못했으며 배포 재현과 구분한다. `KEY-366`에 포함

### KEY-364-F03 — 빈 환자 목록이 제외 범위 자동 발송을 약속함

- 심각도: P1 — 빈 상태 설명과 확정 시연 범위 불일치
- 역할: 스탭·의사
- 경로: 환자 목록에서 처리할 항목이 없는 날짜 선택
- 기대: 환자 등록과 당일 안내문 처리만 안내한다.
- 실제: `확인 문자와 소진 임박 안내는 자동 발송됩니다`라고 표시한다.
- 근거 소스: `frontend/patients.html`
- 상태: 소스 확인만 완료. 실제 빈 날짜 화면에서 재검증 필요

### KEY-364-F04 — 관리 화면에서 미완성 기능을 직접 안내함

- 심각도: P1 — 시연 중 미완성 상태와 제외 기능을 노출
- 역할: 스탭·의사
- 경로: 상단 `관리` → 환자 명단
- 기대: 사용할 수 있는 관리 기능만 안내한다.
- 실제: `재진 안내 발송은 준비 중입니다`가 노출된다.
- 근거 소스: `frontend/manage.html`
- 상태: 재개 후 배포 스탭 `관리` 화면에서 직접 재현. 본 대화에 캡처 표시

### KEY-364-F05 — 환자 챗봇의 준비 중 문구 (후보)

- 심각도: 미확정 — 실제 환자 화면에서 전화 안내와 동작 확인 필요
- 역할: 환자
- 경로: 안내문 링크 → OTP → 안내문 → 챗봇 열기
- 기대: 병원 연락처 또는 동작 가능한 문의 수단을 제공하거나 진입점을 숨긴다.
- 실제: `문의 창구는 아직 준비 중이에요`라는 개발 단계 표현을 노출한다.
- 근거 소스: `frontend/guide.html`
- 상태: 소스 확인만 완료. 전화 안내가 있으므로 막다른 동선이라고 확정하지 않는다

### KEY-364-F06 — 관리의 환자 피드백 진입점 없음

- 심각도: P1 후보 — KEY-364 점검 범위 7의 필수 동선에 도달하지 못함
- 역할: 배포 스탭 한소영
- 경로: 환자 화면 상단 `관리` (1회)
- 기대: 발송 이력 옆 환자 피드백 탭에서 목록·상세 확인
- 실제: 환자 관리 / 발송 예정 / 발송 이력 탭만 표시됨
- 소스: `frontend/manage.html`, `frontend/js/manage.js`에 해당 진입점 없음.
  별도 `admin-feedback.html`과 목록·상세 API는 존재하나 UI 링크 검색 결과 없음.
- 권한 주의: `AdminPatientFeedbackService._require_admin()`은 `AUDIT_READ`를 요구.
  스탭 권한 확장은 QA가 임의로 하지 않으며 PM/구현 담당자 확인 필요
- 증적: 본 대화의 배포 관리 빈 검색 결과 캡처. 별도 버그 연결은 아직 미완료

## 재개 후 수행 결과

- Jira KEY-364 설명·완료 조건·댓글을 다시 읽고 진행 중 전환 요청.
  이후 상태는 다시 `할 일`이며 자동화가 하위 작업 분리·담당자·기한 지정을 요청함.
- 배포 스탭 기존 세션으로 환자 → 관리 → 환자 등록 → 취소를 직접 클릭해 확인
- 신규 로그인 검증은 아님. 의사·어드민 역할 재검증은 미수행
- 관리 빈 검색 결과: `조회된 환자가 없습니다` 정상 표시, 원시 오류 코드 없음
- 관리 → 환자 등록 1회 접근, 필수 입력/수신 동의가 없는 상태의 등록 버튼 비활성 확인
- 배포 `manage.html` SHA-256이 기준 소스와 일치:
  `b8d3f2771557942a3d91a2127e3086a8ce0ad75876d7f272451f95058e3b9a19`
  한 파일의 일치이며 서버 전체 배포 버전 일치를 의미하지 않음
- 관련 피드백 Node 검사 10건 통과
- `node --test --test-reporter=dot frontend/tests/*.test.js` 전체 실행 종료 코드 0
- 자동 검사 통과는 배포 브라우저 완주를 대체하지 않음
- 기존 합성환자의 수신번호가 사용자 테스트 번호와 달라 승인·문자 발송을 하지 않음
- 새 합성 환자/실제 문자 테스트에 끝자리 7659 번호 사용 여부를 사용자에게 질문함
- 이후 사용자가 동의하여 배포 서버에 `QA-KEY364-20260917` / `시연검증 합성환자`를
  새로 등록함. 수신 동의 및 사용자 끝자리 7659 테스트 번호를 적용했으며 기존 팀원
  환자 정보는 변경하지 않음. 등록 후 OCR 업로드 화면으로 정상 이동함.
- 저장소 SYN-EMS-01 생성기를 사용하고 이름·차트·진료일만 테스트 정보로 바꾼 합성
  EMR PNG를 `/private/tmp/key364-fixture/key364-synthetic-emr.svg.png`에 생성·육안 확인.
  임상 필드는 정본 그대로이며 Quick Look 렌더이므로 KEY-190 고정 PNG와 동일하지 않음.
- 앱의 파일 선택 창 자동 조작이 허용되지 않아 사용자에게 파일 선택을 요청함.
  아직 업로드·OCR·승인·실제 문자 발송은 실행 완료되지 않음.

## 현재 판정

- 통과: 기존 스탭 세션의 환자 목록 → 관리 → 등록 화면 → 취소 동선,
  관리 빈 검색 결과, 프런트엔드 자동 검사.
- 보류: 신규 합성 진료의 업로드부터 환자 OTP·챗봇·피드백·관리 확인까지의 완주.
- 수정본 로컬 통과: D+7 제외 범위 문구·버튼 제거, 미완성 기능 문구 제거.
- 배포 미확인: 위 수정의 `care-on.site` 반영 여부.

기존 D+7 발견만으로 나머지 검증을 중단할 필요는 없다. 다만 배포 전체 흐름의
완주·역할별 확인·수정 재검증·캡처 첨부·버그 연결이 남아 있으므로 완료 판정하지 않는다.
이전 Jira 댓글의 ‘최신 배포/로컬’, ‘추가 P1 확정’ 표현은 위 증거 수준으로 정정한다.

## 2026-09-18 배포 브라우저 재개 결과

- 대상은 별도로 새로 등록한 합성환자 `QA-KEY364-20260917-02`이다. 기존 접수는
  취소하거나 삭제하지 않았다. 사용자가 다시 업로드한 10장으로 진행한다.
- 새 접수는 현재 `판독 확인` 상태다. 진단·처방·처방기간은 선택되지 않았으며,
  화면에는 `확인할 항목 23개 — 못 읽음 23`이 표시된다. 일부 추출값이 화면에
  존재하므로 이 숫자를 실제 추출 실패 개수로 확정하지 않는다.
- 원본 미리보기는 이전에 일부 문서 조회 실패를 관찰했고, 이후 사용자가 표시
  복구를 확인했다. 실제 파일 삭제나 서버 오류의 원인은 확정하지 못했다.
- 미확정 OCR → `안내문` 탭: 1회 클릭으로 이동, 아직 안내문이 없으며 진료기록에서
  확정하라는 메시지를 표시한다.
- `최종 확인` 탭도 같은 안내 메시지를 표시하며 승인 기능을 노출하지 않는다.
  `진료기록` 탭 1회 클릭으로 OCR 화면에 복귀했다.
- 이번 재개에서는 임상값 저장, OCR 확정, 안내문 생성, 의사 승인, 문자 발송을
  실행하지 않았다. 여러 날짜의 원문을 현재 처방으로 임의 적용하지 않기 위해
  시연에 사용할 처방기간(통/일)을 사용자에게 확인 요청했다.
- 위 항목은 배포 스탭 기존 세션의 UI 확인이며, 신규 로그인·전체 E2E 통과 또는
  배포 전체 버전 검증을 의미하지 않는다.

### 같은 날 추가 관리 동선 검증

- OCR 화면 상단 관리 링크 → 관리 화면: 1회 클릭으로 접근했다.
- 관리에는 환자 관리 / 발송 예정 / 발송 이력만 표시됐다. 환자 피드백 탭은
  여전히 없어 F06은 해결되지 않았다. 별도 URL을 통한 접근을 정상 동선으로
  대체하지 않았다.
- 숫자 없는 존재하지 않는 이름으로 검색 시 `조회된 환자가 없습니다`,
  `전체 0명`을 확인했다. 숫자가 섞인 검색어는 전화번호 부분 검색과 매칭될 수
  있어 빈 결과 검증에 쓰지 않았다.
- `재진 안내 발송은 준비 중입니다` 문구가 남아 있어 F04를 재현했다.
- 발송 예정 / 발송 이력 탭은 각각 1회 클릭으로 전환됐다.
- 발송 이력은 `2026-09-12 ~ 2026-09-18 발송된 문자가 없습니다`를 표시했다.
- 다른 진료의 시각 변경, 원본 삭제 재시도, 문자 재발송, CSV 다운로드는
  실행하지 않았다. 이 확인은 해당 합성 진료의 발송 성공을 의미하지 않는다.

### 처방 입력 재확인 및 경고 문구 (이전 미입력 기록 갱신)

- 사용자가 변경한 화면을 다시 확인했다. 합성 차트 `QA-KEY364-20260917-02`에
  진단 PCOS, 약속처방 `PCOS · 야즈 (계속)`, 수동 약품명 `야즈`, 기간 `2`,
  단위 `통` 및 `저장했습니다` 메시지가 표시된다. 이전의 처방기간·약품명
  미입력 기록은 이 시점의 상태에 적용하지 않는다.
- 저장 후에도 `처방일수가 28일 미만입니다 — 총투가 일수인지 확인해 주세요`
  경고가 표시된다. 단위 반영 누락인지 오래된 경고인지는 미확정이다.
- 수동 약품명과 함께 `이 처방에 등록된 약이 없습니다` 문구도 표시된다.
  약속처방 카탈로그의 약품 목록과 현재 진료의 수동 입력 약품은 별개이므로,
  이 문구만으로 현재 진료의 약품 누락 또는 DB 오류라고 단정하지 않는다.
- 기대: 기간 경고는 단위를 반영해야 하고, 카탈로그 등록 상태와 진료 입력
  상태를 문구에서 구분해야 한다. 심각도는 생성·환산 영향 확인 후 확정한다.
- 배포 스탭 화면 관찰이며 DB 영속값·실제 생성 결과는 미검증이다.
  KEY-364에 같은 내용의 추가 QA 댓글을 등록하고 저장 완료를 확인했다.

## 2026-09-18 로컬 재현 및 수정

- 로컬 `192.168.219.103:8080`에서 `유가탱` 진료를 다시 열어 `2통`인데도
  `처방일수가 28일 미만입니다` 경고가 표시되는 것을 재현했다.
- 로컬 관리 화면에는 `환자 피드백` 탭이 없었고, 제공 중인 `manage.js`의 해시도
  최신 `develop`과 달랐다. 로컬 실행본이 최신 소스가 아님을 별도로 확인했다.
- 작업 브랜치를 `develop` `726f795`로 fast-forward한 뒤 다음을 수정했다.
  - 프런트 처방기간 계산이 백엔드 `course_days()`와 동일하게 단위를 반영한다.
    `2통`은 56일로 계산하며, 28 이상의 통 단위 옛 값은 이미 일수인 값으로 본다.
  - 약속처방의 기본 약이 없는 상태와 현재 진료에서 직접 입력한 약을 안내
    문구에서 명확히 구분한다.
  - 후속 문자 데이터·저장·발송 코드는 유지하고, 확정 시연의 문자 설정과 현황
    화면에서는 진료 당일 안내문만 표시한다.
  - 빈 환자 화면과 관리 화면에서 시연 범위 밖 기능 및 준비 중 문구를 제거한다.
- 관련 테스트 191건 통과 후 프런트 전체 테스트를 실행해 모두 통과했다.
- 기존 로컬 스택과 사용자 작업을 보존하기 위해 수정본 정적 파일만 별도
  `8081` 미리보기 컨테이너로 실행했다. API·DB는 기존 로컬 스택을 공유한다.
- 의사 역할로 실제 화면을 다시 확인한 결과는 다음과 같다.
  - 문자 설정에는 진료 당일 안내문과 당일 발송 시각만 표시되며 D+7·보름·소진·
    재진 회차는 표시되지 않는다.
  - 현황에는 진료 안내문 발송과 안내문 읽음만 표시되며 확인 문자 응답 영역은
    표시되지 않는다.
  - OCR의 기간 `2`, 단위 `통` 조합에서 28일 미만 경고가 표시되지 않는다.
  - 약속처방 기본 약과 현재 진료 직접 입력 약을 구분한 새 문구가 표시된다.
  - 관리 화면에 환자 피드백 탭이 표시되고 준비 중 문구는 표시되지 않는다.
- 피드백 탭 진입 시 기존 로컬 API 이미지가 `403`을 반환했다. 미리보기 API는
  수정 기준 소스보다 오래된 이미지이며 `PATIENT_FEEDBACK_READ` 권한이 없다.
  수정 기준 소스에는 스탭·의사·관리자 권한이 정의되어 있고 피드백 API·RBAC
  테스트 155건이 통과했다. 따라서 이 403은 이번 프런트 수정의 실패가 아니라
  오래된 실행 이미지 차이로 기록한다.
- 프런트 전체 Node 테스트와 피드백 API·RBAC 테스트가 모두 통과했다. 다만
  환자 등록부터 실제 문자·OTP·챗봇·피드백 저장·관리 조회까지의 단일 E2E와
  `care-on.site` 배포 재검증은 여전히 남아 있으므로 KEY-364 전체 완료로 판정하지
  않는다.
