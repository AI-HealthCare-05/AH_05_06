# KEY-314 안내문 출력 정제 검증 기록

최종 갱신: 2026-09-19

기준 브랜치: `codex/KEY-314-guide-output`

기준 develop: `54651baf`

## 선행조건

- KEY-277 생성 연결과 근거 스냅샷은 develop에 병합돼 있다.
- KEY-323의 PCOS Monash 범위와 자궁내막증 ESHRE 고정 템플릿 계약을 유지한다.
- KEY-339는 Jira에서 완료·해결됨으로 확인했다.
- 자궁내막증 생활관리 고정 템플릿은 모델 정제·길이 제한·중복 검사 대상이 아니다. 승인 본문과 checksum을 그대로 사용한다.

## 구현한 출력 계약

| 구분 | 정제 규칙 |
|---|---|
| 공통 말투 | 환자에게 직접 말하는 존댓말, 공포·단정 표현 배제, 어려운 용어는 쉬운 말로 설명 |
| 문장·형식 | 짧은 문장, 섹션별 제목, 짧은 문단 또는 `-` 불릿, 한 불릿에 한 내용 |
| 중복·길이 | RAG 생성 섹션당 1,500자·불릿 6개 상한, 반복 문장/불릿은 저장 전에 거절 |
| 복약지도 | 복용 이유와 환자가 기억할 핵심에 집중. 별도 처방 사실을 되풀이하거나 스케줄을 새로 만들지 않음 |
| 주의사항 | 흔한 반응과 의료진에게 알려야 할 상황을 구분 |
| 생활관리 | 승인 근거가 있는 일상 관리만 안내. PCOS는 특정 식이·운동 방식의 우월성을 주장하지 않음 |
| 근거 표시 | 의사 화면에서 생성 방식, 출처 기관, 문서, 버전, 확인일, URL을 항목별 표시 |
| fallback 표시 | 승인 고정/검색 장애/근거 없음 사유와 템플릿 ID·버전을 RAG와 구분 |

## 두 질환 정상 시나리오 비교 기준

실제 환자정보는 사용하지 않는다. 아래 표는 모델 본문을 꾸며 낸 결과가 아니라, 승인 자료로 생성한 출력을 비교할 때 적용하는 판정표다.

| 질환 | 생성 경로 | 정제 전 확인할 문제 | 정제 후 합격 기준 |
|---|---|---|---|
| PCOS | Monash 승인 청크를 사용하는 RAG | 장문, 같은 생활 습관 반복, 특정 식단·운동의 우월성 암시, 출처가 본문에 섞임 | `[생활관리]` 목적에 맞는 존댓말, 1,500자/6불릿 이내, 중복 없음, 우월성 주장 없음, 근거는 의료진 화면에만 표시 |
| 자궁내막증 | ESHRE 승인 고정 템플릿 | RAG 출력과 다르게 보이더라도 모델이 문구를 다듬거나 자르면 checksum 훼손 | 승인 템플릿 본문이 문자 단위로 보존되고, `fixed_approved_template`·템플릿 버전·기관·문서·확인일이 표시됨 |

## 자동 검증

- 전체 `ruff check .`: 통과
- 전체 `mypy app`: **469 source files**, 오류 없음
- 전체 Python: **3,055 passed, 8 skipped, 1 xfailed, 14 subtests passed**
- 전체 프런트 Node: **1,409 passed**, 실패·skip 없음
- KEY-314 + KEY-277 생성 + KEY-323 집중 회귀: **65 passed, 1 skipped**
- 테스트는 현재 스키마로 매번 새 검사 DB를 만들어 실행했다. 기존 로컬 앱 DB는 변경하지 않았다.

## 실제 샘플·브라우저 확인 상태

- 로컬 앱 DB에는 승인 현재 버전 5건(ESHRE 2022, Monash 2023, 식약처 3건)이 있으나, `chunk_optional`·`deprecated_at` 컬럼이 없는 구 스키마다. 최신 생성 파이프라인의 실제 샘플 근거로 사용할 수 없어 임의 migration이나 승인 조작을 하지 않았다.
- 2026-09-18 SSH timeout은 Pilot 인스턴스 증설 당시 바뀌기 이전 퍼블릭 IP로 접속해 발생했다. 현재 주소는 탄력적 IP로 고정됐으며, 공개 저장소에는 주소를 기록하지 않고 팀 비공개 채널의 접속 정보를 사용한다.
- 2026-09-19 현재 Pilot의 `GUIDE_RAG_ENABLED=false`이다. 따라서 현재 Pilot 생성은 RAG 출력 정제 실측이 아니라 고정 템플릿 경로 확인에만 사용한다.
- PCOS RAG 정상 생성 실측은 질의 개선 #366·#367이 병합되고 5차 배포에서 RAG를 다시 켠 뒤 수행한다. 현재 질의는 유사도 0.53으로 fallback 경로를 탄다.
- 자궁내막증 고정 템플릿은 Pilot에서 정상 동작하며, 승인된 시연용 visit 115를 후속 실측 대상으로 사용할 수 있다.
- 따라서 이 문서에는 합성 모델 출력을 실제 RAG 샘플처럼 싣지 않았다. RAG 재활성화 후 PCOS 정상 생성 1건과 자궁내막증 고정 템플릿 1건을 위 판정표와 의사 화면 근거 카드로 수동 확인한다.
- 실제 샘플 확인 전에도 안전 계약은 자동 회귀로 검증한다. 처방 외 약물, 복용 변경·중단 권고, 미승인/충돌/검색 장애/LLM 실패 경로는 기존 KEY-83·KEY-127·KEY-277 테스트를 그대로 통과해야 한다.
